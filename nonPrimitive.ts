// array , object

// ts - tuple

let bazarList: string[] = ["eggs", "milk", "sugar"];

// bazarList.push(12)

let mixedArr: (string | number | boolean)[] = [
  "eggs",
  "one dozon",
  "milk",
  "2 kg",
  "sugar",
  3,
  true,
];

let coOrdinates: [number, number] = [15, 23];
// coOrdinates[1] = 'wrong';

let person: [string, number, boolean] = ["hridoy saha", 20, false];

let destination: [string, string, number] = ["Dhaka", "Chattogram", 2];

let user = {
  name: "hridoy",
  roll: 22,
};

// console.log(user.location);
